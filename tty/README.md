```bash
# Use 'esc' as 'caps_lock'.
# Use 'windows button' as 'meta+space'.
/usr/share/kbd/keymaps/i386/qwerty/uk-custom.map.gz

# Set a font.
# Apply the custom keymap.
/etc/vconsole.conf

# Auto network configure assuming systemd-resolved service is enabled.
/etc/iwd/main.conf

# Double the font size.
# Stop the cursor blinking.
/usr/local/bin/tty-startup.sh

# Run `tty-startup.sh` on startup.
/etc/systemd/system/tty-startup.service
```
