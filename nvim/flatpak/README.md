# Usage

1. Ensure an nvim config is available on the host machine
2. Run `./package.sh` on host machine
3. Upload the tarball somwhere
4. Ensure flatpak is available on the target machine
5. download and untar the tarball on target machine
6. Run `./install.sh` on the target machine
